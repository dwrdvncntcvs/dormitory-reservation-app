import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aredna.app',
  appName: 'aredna',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
